
const DeleteButton = (onHandleDelete, text) => {
    return (
        <button
            onClick={onHandleDelete}
            type={}
            className={}
        >
            {text}
        </button>
    );
};

export default DeleteButton;